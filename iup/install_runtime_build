echo This script will install the Run Time libraries in the system
echo from the build directories. It was tested only in Ubuntu.
echo .
echo Must be run with "sudo", or install will fail,
echo and must specify the platform name in the command line,
echo for example:
echo    cd iup
echo    sudo install_runtime_build Linux26g4

cp -f lib/$1/*.so /usr/lib
