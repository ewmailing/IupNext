echo This script will install the Development files in the system
echo from the build directories. It was tested only in Ubuntu.
echo .
echo Must be run with "sudo" at the library folder, or install will fail,
echo and must specify the library name and platform name in the command line,
echo for example:
echo    cd iup
echo    sudo install_dev_build iup Linux26g4

cp -fR include/* /usr/include/$1
cp -f lib/$2/*.a /usr/lib
